!!! XML
!!! 5
%html
  %head
    %meta{ "http-equiv" => "content-type", :content => "text/html; charset=utf-8" }
    %title= ChefServerWebui::Application.config.title_string
    = stylesheet_link_tag "application", :media => "all"
    = javascript_include_tag "application"

  %body
    #container
      #header
        %h1= link_to ChefServerWebui::Application.config.title_string, top_path
      #wrapper
        #main.loginPage
          - unless flash.empty?
            .block#block-messages
              .content
                %h2.title Messages
                .inner
                  .flash
                    - flash.each do |type, msg|
                      %div{:class => "message #{type}"}
                        %p= msg
          = yield
          #footer
            .block
              %p= raw("Copyright &copy; 2009-#{Time.now.year} Opscode")
        #sidebar
          .block.notice#sidebar_block_notice= yield :sidebar_block_notice
          .block#sidebar_block= yield :sidebar_block
        .clear
